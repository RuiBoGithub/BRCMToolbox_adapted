based on
_development/RC_br/ETHlib
and its example file
_development/RC_br/_version_MAY26_debug.ipynb

TRY TO ENABLE A WORKFLOW OF BRCM based conversion connected to 5R1C workflow. Note that previously we have defined based on json for 5R1C and keep this.
Note that all the implementation is preferred to be stored under _development/.. (a new folder)
1. Read the EnergyPlus IDF and parse zones, surfaces, constructions, materials, windows, and schedules.
2. Convert these into a common normalized building-data structure.
3. Derive shared physical quantities: areas, volumes, U-values, thermal masses, ventilation/infiltration, and boundary conditions.
4. Based on the information shown in . Pass the normalized data to a `generate_5R1C()` function to aggregate parameters into the fixed 5R1C topology.
5. Pass the same normalized data to `generate_BRCM()` to construct the detailed multi-node RC network.
6. Apply identical weather, gains, controls, and initial conditions to all models.
7. Simulate and compare temperatures, energy use, calibration performance, and ECM-saving predictions. 